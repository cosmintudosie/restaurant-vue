name: restaurant-vue
type: web service
steps:
  - name: Install dependencies
    command: npm install
  - name: Build
    command: yarn build
buildCommand: yarn build
startCommand: yarn start
env:
  - key: NODE_ENV
    value: production